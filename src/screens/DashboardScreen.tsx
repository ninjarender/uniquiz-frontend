/** T1 shell — filled with the sidebar, hero and bank grid in task fe-0003. */
export function DashboardScreen() {
  return (
    <div className="grad-bg flex h-full items-center justify-center text-white/80">
      Кабінет — у роботі (fe-0003)
    </div>
  );
}
