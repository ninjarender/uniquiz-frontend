# Backend origin for a split deploy (e.g. https://api.uniquiz.example).
# Empty = same-origin: dev goes through the Vite proxy (vite.config.ts),
# prod through a shared reverse proxy. Socket.IO (0051+) uses the same value.
VITE_API_URL=
